#include "buzzer.h"

volatile BuzzerStatus cdt;

void Buzzer_Init(void) {
    HAL_TIM_PWM_Start(&htim3,TIM_CHANNEL_4);
}
void Buzzer_ON(void) {
    //¿ªÆô·äÃùÆ÷
    cdt.Buzzer = 1;
	 __HAL_TIM_SET_COMPARE(&htim3,TIM_CHANNEL_4,10);
}

void Buzzer_OFF(void) {
    //¹Ø±Õ·äÃùÆ÷
    cdt.Buzzer = 0;
    __HAL_TIM_SET_COMPARE(&htim3,TIM_CHANNEL_4,100);

}
